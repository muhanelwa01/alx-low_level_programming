#include "main.h"
#include "5-main.c"

/**
 *printSign:Function prints sign of a number
 *
 *Return:0
 */
void printSign(void)
{
  int c
    if(c < '0');
  _putchar(-);
  while(c > '0');
  putchar(+);
  else(c = 0);
  putchar(0)
    }
return 0
